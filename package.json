{
  "name": "career-track",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.3.0"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "apps/api/**/*.ts": [
      "npm run lint -w apps/api"
    ]
  }
}